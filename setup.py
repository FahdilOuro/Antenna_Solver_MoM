from setuptools import setup, find_packages

setup(
    name='Antenna_Solver_MoM',
    version='0.1.0',     # Mettez à jour la version une fois que on fait une nouvelle mise a jour
    packages=find_packages(),
)
